const byteSize = (str) => {
  // write your code here
	// empty string--> size 0 
	let count = 0
	if(str == ''){
		return 0
	}
	else{
		let arr = str.split('');
		count = arr.length;
		// for(let i=0; i<n ; i++) {
		// 	count++
		// }
		// return count
		
	}
	
};

// Do not change the code below
// const str = prompt("Enter some string.");
// alert(byteSize(str));
